{
  "name": "tether-web",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run build && wrangler dev",
    "deploy": "bun run build && wrangler deploy",
    "pages": "bun run build && wrangler pages deploy pages --project-name=read-tether"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "fountain-js": "^1.2.4",
    "wrangler": "^4.59.2"
  }
}